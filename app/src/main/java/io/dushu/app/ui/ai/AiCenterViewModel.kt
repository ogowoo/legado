package io.dushu.app.ui.ai

import android.app.Application
import io.dushu.app.base.BaseViewModel

/**
 * AI中心ViewModel
 */
class AiCenterViewModel(application: Application) : BaseViewModel(application) {

    // 可以在这里添加AI中心的数据获取逻辑
    // 如：获取最近使用记录、统计信息等

}
